# pf-v3-overnight backlog

One item per iteration, top-most unchecked first. Items are sized to finish inside one iteration; split further in place if needed (complete the first slice). Mark `[x] (sha)` or `BLOCKED(reason, attempt N/3)`. REVIEW items audit; they do not author.

## P0 — v2.x defects (small, independent; ship first)

- [x] (98b7bc3) 14a (#14): Phase 3 ordering fix in SKILL.md — any edit after the pattern pass (cuts, red-team fixes) re-triggers scan + humanizer; copy-editing outputs a changelog; collapse the redundant only-cuts pass into copy-editing. Add the humanizer meaning-preservation guardrail from the issue comment (proposes/arbitrates, one pass, accepted-WARN-with-reason, claim-changing rewrite = defect). TESTS.md entry.
- [x] (2f282bf) 14b (#14): Build-mode verbatim-copy protection — SKILL.md Phase 5 + Gate 2: snapshot approved copy at end of Phase 3 (artifact file), diff rendered text after any builder, drift fails Gate 2. TESTS.md entry. Close #14 with evidence.

## WS1 — conversion contracts (#11)

- [x] (cc7d546) 11a: archetypes.md head rewrite — replace "archetype fixes section order" doctrine with the six-block contract schema (Goal / Buyer entry states / Jobs / Proof requirements / CTA policy / Ordering constraints) + the ~10 ordering invariants extracted verbatim from conversion-rules; shared rules updated so rule 4 and rule 11 now WIN. Phase 2 text in SKILL.md updated to compile-a-contract language.
- [x] (5d54185) 11b: Convert oss-project, saas-homepage, campaign-landing to contracts (every load-bearing rule from plans/research/pf-archetypes.md §1 survives as constraint or job; slot numbers die; awareness-state notes added).
- [x] (f3d22bd) 11c: Convert mobile-app, course-sales, membership-community to contracts (same standard).
- [x] (e76a19e) 11d: Convert newsletter-capture, personal-home to contracts; promote the thank-you page requirement into a shared cross-archetype reference stub (full contract comes in 12a).
- [x] (f966ae8) 11e: Mapper → contract compiler — +awareness state +price/decision weight inputs; output is a filled contract; straddle = merge (union of jobs, conversion owner sets goal, strictest policy); nothing-fits = hand-filled contract; reads foundry-log to adjust axis defaults. Micro-dogfood: compile a contract for a sample brief (a dev-tool + a course) and commit both outputs to loop/evidence/.
- [x] (711fa66) 11f: Composition axes (narrative shape / hero form / proof strategy / density) + section-shape lexicon (3+ shapes per job) as a new reference section; Phase 2 multi-skeleton generation (2–3 constraint-satisfying candidates, MECLABS pre-read, pick at spec sign-off). Dependency from 11e: once the axes section exists, extend the compiler's output paragraph so the filled contract recommends a setting per axis with one reasoning line each (plan lines 25–26); 11e wrote the log-moves-defaults rule in plain language awaiting these defined terms.
- [x] (40f6283) 11g: Anti-template check — foundry-log gains skeleton line (job order + axes); Gate 1 check comparing to the property's last N pages; converged-without-data flagged. TESTS.md entries for 11a–g. Close #11 with evidence.
- [x] (dfbc496) REVIEW-1: fresh audit of everything since start vs #14/#11 acceptance + TESTS.md; reopen failures at top; tag v3-night-1 on green. GREEN — audit record in loop/evidence/review-1.md; tagged v3-night-1.

## WS2 — evidence-based orchestration (#13)

- [x] (02201d6) 13a: Companion table → I/O contract table (INPUT / OUTPUT / CONSUMER / EVIDENCE columns) + two-tier policy per the decision comment: core tier REQUIRED (product-marketing, customer-research, marketing-psychology, cro, copywriting, frontend-design, humanizer; preflight stops until installed; explicit per-run override only, output loudly marked partial); enhancer tier degrades. theme-factory removed from the table (dedup decision).
- [x] (081f952) 13b: Run artifact directory `.agents/foundry/<product>/` (voc.md, persuasion-map.md, message-architecture.md, page-spec.md, conversion-audit.md, tokens/, copy/) wired through every phase: each phase names what it reads and writes. Phase 0 adopts v2.x artifacts (old .agents/product-marketing.md, foundry-log) — this is #19's migration piece, do it here.
- [x] (531dd3f) 13c: Gate-report evidence lines per companion (humanizer-style) + VOC provenance schema (every quote carries a source; Gate 2 tracing + Gate 8 become mechanical checks against voc.md).
- [x] (a3dbf69) 13d: Independent MECLABS audit (Gate 1 scored by a fresh subagent given only rendered page + brief; divergence recorded) + multi-candidate generation (hero candidates by objection framing; frontend-design always 2–3 token plans; selection reasons logged to artifacts).
- [x] (0d8b42d) 13e: Timing fixes — ai-seo answer-block + analytics conversion event into Phase 2 spec; customer-research during intake; competitor-profiling into Phase 0/1 (competitors stays archetype-side); pricing output generates /pricing.md. Close #13 with evidence. TESTS entries 13a–e.
- [x] REVIEW-2: audit WS2 vs #13 acceptance; tag v3-night-2 on green. AUDITED: body deliverables all verified; one decision-comment deliverable unshipped, reopened as 13f; no tag until it closes. Record: loop/evidence/review-2.md.
- [x] (f6aee06) 13f (#13 reopened): absorb impeccable's two scanner rules per the dedup decision (aphoristic cadence; repeated section kickers) into references/voice.md scan:patterns as WARN-level entries, extending scripts/voice_scan.py only if a rule cannot be a line regex (kicker repetition is cross-section, so scanner logic may be needed); TESTS.md entry; re-close #13 with evidence.
- [x] (d12bc4b) REVIEW-2b: confirm 13f meets the dedup decision line and broke nothing (verify.sh, scans, TESTS); tag v3-night-2 on green. GREEN, zero reopens; tagged v3-night-2. Record: loop/evidence/review-2b.md.

## WS3 — impeccable (#15, #16)

- [x] (83d2d0b) 15a: impeccable companion entry (pinned pbakaus/impeccable, detection paths + npx, version from frontmatter, enhancer tier) + PRODUCT.md handoff (write from our brief BEFORE any impeccable command; register brand, platform web, CTA/belief-ladder/proof → Conversion & proof; anti-references verbatim; singleton policy merge-never-clobber) + hooks-off-during-runs note + SECURITY.md boundary paragraph. Also absorbed the issue's unowned optional-invocations deliverable (critique/live/overdrive doctrine); document + detect stay with 15b.
- [x] (5626ce3) 15b: Phase 4 wiring — /impeccable document persists property DESIGN.md (published 8-section spec profile), palette.mjs greenfield seed; design detect gate: detect.mjs --json over built HTML/CSS at Gate 5, findings-are-floors doctrine, per-property accepted-findings file. Micro-dogfood: run the local clone's detect.mjs against docs/index.html, commit output as evidence. Close #15. (Docs findings from the dogfood → #22, seeded before 19b.)
- [x] (27db0bc) 16 (#16): Absorb the 15 craft rules into design-direction.md with provenance stamps — spot-check each constant against the impeccable reference files in the local clone first (critique.md flag); record the MECLABS-over-Nielsen tension note. Close #16 with evidence.
- [x] REVIEW-3: audit WS3; tag v3-night-3 on green. GREEN, zero reopens; 8 craft constants independently re-verified against the clone; tagged v3-night-3. Record: loop/evidence/review-3.md. 3 observations (19a must name WS3 regressions; DOGFOOD-FULL detect from repo root; #22 stays open).

## WS4 — handoff 3.0 (#17)

- [x] (872c238) 17a: handoff.md rewrite — manifest 00–06 (+assets/manifest geometry), five non-negotiables + explicit creative-license grant in 00, fabrication ban verbatim, 01 image slots with dimensions/aspect ratios, 04 from a FIXED criteria template with criterion-ID↔gate-ID map incl. Gates 6+7, 05 voice-rules excerpt, 06 return-spec + revision-request format.
- [x] (fe62776) 17b: design.md projection — generation rules (deterministic compilation from 00–05, published 8-section spec, version alpha pinned, never hand-edited, regenerate on change) + `npx @google/design.md lint` wiring when available + per-tool packaging table (attachments / single-prompt / repo). Closed #17 with evidence.
- [x] (62832e3) REVIEW-4: audit WS4; tag v3-night-4 on green. GREEN, zero reopens; lint claim reproduced cold (exit 0, identical findings); 46-rule count verified by hand in the pinned clone; unowned walkthrough commit audited clean, process lesson recorded. Record: loop/evidence/review-4.md. 4 observations (G5 label drift for #18/19a; N/A body-text nuance for DOGFOOD-FULL; 19a must cover per-tool seam + lint dispositions; unowned commits route through fix_plan).

## WS1b + WS2c — new archetypes and catalog integration (#12, #21)

- [x] (f7d1c8a) 12a: Contracts for pricing-page, comparison-alternatives, thank-you-post-conversion (top-3; thank-you referenced by all archetypes).
- [x] (891f536) 12b: Contracts for docs-dev-tool-landing, waitlist-coming-soon, event-webinar (Event schema note).
- [x] (7020a26) 12c: Contracts for agency-services, ecommerce-product, changelog-launch-post; 404 note in shared rules; portfolio = personal-home axis note. TESTS.md entries for 12a-c, including extending test 38's public-pricing enumeration with pricing-page. Close #12.
- [x] (40f2e57) 13g: customer-research full-capability fix — Phase 0/1 and the companion-table INPUT currently authorize only Mode 1 (analyze sources the brief points to). Authorize Mode 2 explicitly: when pointed-to VOC sources are thin or absent, customer-research runs its digital-watering-hole research (Reddit, G2, HN, communities per its own source-selection table) to FIND buyer language, with findings landing in voc.md with sources like any other quote; the interview then asks only what discovery could not supply. Note the outbound-web nature in the phase text. TESTS entry.
- [x] (1d22a6b) 13h (audit F5): voc.md gains customer-research's synthesis fields per quote (theme, confidence label, date, frequency-x-intensity note); Phase 1 invocation names the ranked-theme deliverable BEFORE hierarchy/objection ordering so one loud old quote cannot steer the hero. TESTS entry.
- [x] (f3ce132) 13i (audit F6): page title + meta description are copy — Phase 3 step 2 requests copywriting's meta output, routes it through the full voice chain, lands it in copy-approved.md; Gate 6 verifies what the snapshot holds instead of checking unvoiced meta. TESTS entry.
- [x] (9da1dcd) 13j (audit F7): the "my page isn't converting" rewrite path diagnoses before re-deriving — intake invokes cro's live-page analysis on the existing URL and records a data pull (analytics/search/heat, or an explicit "no data exists" line) into the brief before Phase 1. TESTS entry.
- [x] (5126fa9) 13k (audit F8): token plan requires a layout grid spec (containers, columns, gutters) plus per-breakpoint recomposition notes for the hero and densest section (design-direction.md); Gate 5 verifies against it.
- [x] (b73f2b4) 13l (audit F9): cro form.md invoked archetype-conditionally (newsletter-capture, waitlist, event-webinar, campaign-landing and any form-centric spec), same pattern as pricing/aso; Gate 1 reads its output.
- [x] (5d40dff) 13m (audit F11): name the unused capabilities in invocation text — Phase 1 objection map + Phase 3 red-team consume the brief's Switching Dynamics + anti-persona sections; Phase 2 pricing sections apply pricing psychology even when the pricing enhancer is absent (conversion-rules fallback); Phase 2 names Fogg/Hick as spec-structure inputs from marketing-psychology; copy-editing invocation names its So-What and Specificity sweeps, not cut-only.
- [x] (970d6f9) 13n (audit F12): Phase-2 ordering inversions — multi-page properties emit site-context.md (site-architecture skill) BEFORE contract compilation as a compiler input (nav policy, pricing-shape fork, split-vs-straddle); PRODUCT.md written at spec sign-off, Phase 4 demoted to merge-never-clobber re-verify. FOLD INTO 21a: when you reach 21a, its site-architecture placement follows THIS item, not the original adjunct wording. NOTE for 21a: the site-architecture companion-table row (enhancer tier) shipped in 13n; 21a's remaining site-arch scope is the scope-escalation seam + cross-verification.
- [x] (37e8362) 13o (audit F2-separable): wire impeccable critique->polish: at the Gate 5 fix loop, a failed render review runs /impeccable critique, and its scored snapshot is polish's backlog, per impeccable's own design.
- [x] (c3dd81c) 13p (audit F3-separable): Phase 5 deliverables include the OG image (1200x630) and favicon Gate 6 already checks — produced, not just verified; real artifacts under the integrity rule.
- [x] (4a0776a) AUDIT-F1a (owner-approved): hoist per-job shape selection into Phase 2 — the contract compiler recommends a shape per job from the section-shape lexicon, spec sign-off confirms them, and Phase 4 step 3 demotes to confirm-or-override where an override is a recorded decision knowingly entering the re-trigger loop. Edits: SKILL.md Phase 2 + Phase 4, archetypes.md compiler output. TESTS entry.
- [x] (7adedb0) AUDIT-F1b (owner-approved): bounded copy-fit pass between Phase 4 and Phase 5 — one planned pass with the chosen direction's mockup in hand: hero finalist re-picked against the real hero form, shape-fit confirmed per section, length trims made; edited sections take ONE planned re-entry through the voice chain (scan + humanizer), and THEN copy/copy-approved.md is written (the snapshot moves from Phase 3 step 6 to the end of this pass; Phase 3 keeps drafting + approving, the freeze happens once form exists). Gate 2's verbatim diff unchanged. Update the Phase 3/5 artifact lines. TESTS entry.
- [x] (4ab14b7) AUDIT-F2 (owner-approved): Phase 4.5 comp stage — after tokens + shapes, render the full page as a comp and critique it against the direction's own scene sentence and named anchor references ("does this belong in their league?"); max 2 bounded rounds with authority to bounce to Phase 4; with impeccable installed its critique command runs the review and the scored snapshot is the round's record. The comp feeds the copy-fit pass (F1b) so fit happens against a real comp. SKILL.md new section + ship-gates cross-ref. TESTS entry.
- [x] (baaa1aa) AUDIT-DOCS: fold the restructure into the record — plans/v3.0.md gains the audit outcome + approved changes; plans/orchestration-walkthrough.md regenerated to show Phase 2 shape recommendations, Phase 4.5 comps, and the moved snapshot; both scanner-clean.
- [x] (14eae08) 21a (#21): site-architecture (Phase 2 adjunct + scope-escalation seam), competitor-profiling (already in 13e — verify + cross-reference), seo-audit (Gate 6), ab-testing (post-ship loop) — real I/O contracts in the table + phase bodies. Evidence comment on #21; issue stays open for 21b.
- [x] (c09d6f9) 21b (#21): image + canvas-design (asset ownership split), webapp-testing (Gates 3/5 executor), gstack diagram (how-it-works), motion stack rewrite (remotion-best-practices + create + render + captions; hyperframes flagged evaluate-before-trust), react-view-transitions conditional, deploy-to-vercel opt-in; distribution seam section (launch orchestrates social/pr/ad-creative/emails/directory-submissions consuming run artifacts, opt-in post-ship); onboarding companion entry (thank-you-post-conversion section companion; plan line 45 catalog item, previously unowned — added by 12a per lesson 13). Close #21. DONE: all pinned names verified live (two upstream renames caught, one disappearance); free-tools/paywalls promoted per matrix wording (OWNER-REVIEW); 4 undispositioned upstream names routed to #6.

## WS5 + WS6 — gates, docs, dogfood, release (#18, #19)

- [x] (b08bc5e) 18 (#18): Gate upgrades consolidation — squint test in render review, focus/hover + CLS/LCP teeth in Gates 3/4, floors-not-verdicts doctrine line, final gate-report format (evidence lines + humanizer + degraded + skeleton + design-detect). Close #18.
- [x] (74c79ef) 22 (#22): docs detector findings — restyle docs/ (numbered section markers, side-tab accent) or record each as an accepted finding with a real reason in a repo `.impeccable/config.json`; re-run detect and commit the output as evidence. Close #22. (Discovered in 15b's micro-dogfood; must precede 19b's docs truth pass.) DONE: side-tab restyled, numbered-markers accepted with reason, comp-round fade treatment + hover underlines shipped; detect `[]` exit 0 with counter-probes. NOTE for DOGFOOD-FULL: interceptor screenshot/eval were down this iteration (evidence in 22-docs/notes.md); health-check capture before relying on it for the 390/1440 renders.
- [x] (626bdaf) 19a (#19): TESTS.md full sweep — regression entries for every v3 behavior (contract compiler, required-core preflight, artifact directory, evidence lines, verbatim diff, detect gate, handoff manifest, projection). Renumber cleanly. DONE: five behaviors already owned (mapped), three gaps filled as tests 65-67 with file:line provenance; entries sorted numerically with labels preserved (external refs stay valid); coverage map in loop/evidence/19a/.
- [x] (07fd764) 19b-1: Homepage (docs/) truth fixes — the three evidence-backed, version-independent findings on docs/index.html, verified by scan + detect + interceptor-open render: (a) unattributed line-89 founder quote reframed to drop the unverifiable attribution while preserving the sameness/motivation claim (evidence loop/evidence/13j-dogfood/); (b) Measurement gate-report cell "GitHub · skills.sh" reworded to name the tracked conversion honestly (installs at skills.sh), no on-page-analytics claim (evidence 13j-dogfood); (c) meta description rewritten to <=160 chars and breaking the three-verb-clause pattern, og/twitter/JSON-LD descriptions aligned pattern-free with the orchestration claim preserved (evidence loop/evidence/21a/ + #19 audit-f1b comment). Re-run impeccable detect (findings-are-floors; accepted list holds) + Interceptor render check (screenshot may be down per lesson 30 — health-check, fall back honestly).
- [x] (b72df1f) 19b-2 (#19): Repo + release-coordinated docs truth pass — README, skill README, CHANGELOG v3.0.0 entry, SKILL.md orientation text, docs/llms.txt; no claims for anything not merged; plans/v3.0.md status flipped to shipped-pending-release; homepage version badge (docs/index.html:48 currently "v2.8"; released is 2.9.1) reconciled to the v3.0 shipping story (coordinate with the CHANGELOG v3.0.0 + status-flip decision; not forward-looking). Note migration shipped in 13b. Close #19 with evidence. DONE: README+skill-README archetype count (eight to sixteen contracts) + theme-factory drop; CHANGELOG v3.0.0 (unreleased) across WS1-6; plan status flipped; badge de-versioned (OWNER-REVIEW in report); llms.txt contract+detector truth; SKILL orientation verified already-correct, no edit. verify.sh GREEN, scans 0 FAIL, interceptor render confirmed.
DOGFOOD-FULL (split iter 52): run the v3 skill end to end in build mode on a real brief, the Interceptor OSS tool (real repo github.com/Hacker-Valley-Media/slop-browser, real source ~/.claude/skills/Interceptor/SKILL.md), working root loop/evidence/dogfood/interceptor/. Slices mirror the pipeline; each is one iteration; DOGFOOD-FULL is done only when DOGFOOD-7 passes the page's own gates. Renders: loop/pf-render.sh <url> <out.png> for desktop 1440 (focuses Chrome + fresh tab; screenshot is NOT down, lesson 30); iframe-eval overflow method for true 390. The page must pass its own gates; fix what fails, that is the point.

- [x] (b10a5e8) DOGFOOD-1: Phase -1 preflight + Phase 0 intake. Companion detection report (present/outdated/missing + tier + what's lost); invoke product-marketing on the real source → product-marketing.md brief (competitive frame from competitor-profiling; unknowns marked [TK], never fabricated); invoke customer-research → voc.md started with real Mode-2 discovery (problem-space buyer language, real sources with URLs) + Themes section. foundry-log.md seeded. All md scanner-clean. Working dir under loop/evidence/dogfood/interceptor/foundry/.
- [x] (88ec57c) DOGFOOD-2: Phase 1 message architecture. customer-research theme ranking finalized; invoke marketing-psychology → persuasion-map.md; write message-architecture.md (positioning sentence, hierarchy ordered by theme ranking, objection map from Switching Dynamics + anti-persona, proof inventory with [TK] for anything unverifiable). DONE: persuasion-map (pratfall primary, Fogg/Hick deliverables, pricing out-of-scope, social-proof declined as [TK]); message-architecture (positioning from frame, 1+4 hierarchy in theme order with recorded ordering note keeping Low-confidence theme 5 out of the lead, O1-O5 objection map covering every Habit/Anxiety force + anti-persona bound, 12 real proofs / 9 [TK]); voc theme ranking finalized, no redundant discovery pass (not thin). All 3 files scanner-clean 0 FAIL/0 WARN; verify.sh GREEN.
- [x] (cabb4aa) DOGFOOD-3: Phase 2 page spec. Contract compile (mapper classifies → archetype, likely oss-project; filled six-block contract + ordering invariants); 2-3 candidate skeletons with MECLABS pre-reads + per-job shape recommendations; section spec; analytics + ai-seo spec entries; PRODUCT.md written at spec sign-off (merge-never-clobber). DONE: classified oss-project (no straddle, business model [TK]); six-block contract + shared-10/oss-3 constraints; axes demo-led/install-command/interleaved/compressed with per-job shape table; 3 legal skeletons cro-scored on MECLABS (A C=31 recommended, B 28, C 30), comparison-early dropped as illegal not padded; S1-S11 section spec; ai-seo answer block (54w, sourced) + analytics measurement plan (honest off-page install event) live; PRODUCT.md gated off (impeccable absent, OWNER-REVIEW); all proof sourced or [TK], scanner clean, verify.sh green.
- [x] (7e2bacf) DOGFOOD-4: Phase 3 copy + Gate 1. Invoke copywriting → full draft + meta title/description; voice chain (word scan → pattern pass → humanizer, meaning-preservation); copy-approved.md snapshot deferred to the copy-fit pass per AUDIT-F1b (Phase 3 approves; freeze after form exists); Gate 1 conversion-audit.md with independent MECLABS score + divergence. DONE: hero-candidates (3 framings, winner detection/theme-1, headline 40 chars), body-draft (full S1-S11 + meta), process (copy-editing changelog + red-team + voice chain: 3 draft WARNs resolved meaning-preserved, humanizer register-arbitration recorded), approved-draft 0 FAIL/0 WARN; copy-approved.md correctly ABSENT (deferred). Gate 1: self C=31 vs independent cold C=18, divergence 13, [TK]-driven (install command + quickstart output deferred to DOGFOOD-6 under the integrity rule), flagged I/F dispositioned, re-score predicted at DOGFOOD-7. verify.sh GREEN.
- [x] (4a364d0) DOGFOOD-5a: Phase 4 design direction. Invoke frontend-design (core, present) → 2-3 token plans differing on a real axis (temperature/type-contrast/density), each with named-hex palette, display/body pairing, grid spec (container max-widths, columns, gutters per breakpoint), one signature element, critique against the generic default; winner chosen with reasons, one file per plan in foundry/tokens/. web-design-guidelines (present) authoritative on a11y/type/imagery/interaction mechanics. Confirm-or-override each per-job shape with tokens in hand (recorded decisions). Per-breakpoint recomposition notes for hero + densest section written into the winning plan. Persist winning tokens to pages/interceptor/theme.css (custom properties incl. --container-max/--grid-cols/--gutter). DESIGN.md: impeccable absent, so the step-6 persistence is skipped-not-substituted per the skill's own impeccable-absent doctrine (theme.css is the design record); record the degraded seam. Micro-dogfood: tokens/ + theme.css exist, scanner-clean, grid + recomposition present.
- [x] (5927150) DOGFOOD-5b: Phase 4.5 comp stage + copy-fit freeze. Render comp/index.html once (tokens, confirmed shapes, approved-draft copy, spec order) and screenshot at 390 (iframe-eval) + 1440 (loop/pf-render.sh) via Interceptor per lesson 30; critique against the scene sentence + named anchors + squint test, bounded 2 rounds, records to comp/rounds.md (impeccable absent → walk critique manually, degraded). Then the copy-fit pass: re-pick hero finalist against the real hero form, confirm shape fit section by section, trim to length, one planned voice-chain re-entry on edited sections, then freeze copy/copy-approved.md (title + meta as labeled entries). Micro-dogfood: renders captured (honest fallback if a viewport blocks), copy-approved.md written scanner-clean. DONE: comp built + 2-round critique (round 1 caught the hero proof-readout invisible under the load animation, fixed in theme.css to transform-only; monotone rhythm fixed); copy-fit confirmed hero variant 1 (wrap deferred to build phrase-break, fallback-font caveat), S5/S7 listified to confirmed shapes, copy-approved.md frozen scanner-clean. Renders via headless-Chrome fallback (screen locked overnight blocks Interceptor captureVisibleTab + pf-render.sh; real pixels, OWNER-REVIEW). verify.sh GREEN.
- [x] (9d2ca1c) DOGFOOD-6a: build-time repo verify + copy-truth reconciliation. Run Phase 5's verify-the-repo-before-it-ships gate against the live repo, resolve the `[TK]` install/license/quickstart slots from real sources only (no fabrication), correct any falsified claim through the voice chain, and re-freeze copy-approved.md. (Split from DOGFOOD-6 iter 58: the dogfood's repo-verify gate caught real drift since the DOGFOOD-1 snapshot; reconciling frozen copy to live truth is integrity-critical prose warranting its own iteration.)
- [x] (2b6fd00) DOGFOOD-6b: Phase 5 build. Build static index.html verbatim from the reconciled copy-approved.md into pages/interceptor/; OG image (1200x630) + favicon produced as real artifacts; llms.txt; theme.css already persisted. Status-line + CTA-block build elements match the reconciled copy (repo name, .pkg-download CTA). DONE: index.html verbatim from the frozen snapshot (33/33 body-copy lines; title+meta in head for Gate 6; readout status card is a documented list-render boundary); real 1200x630 og.png via headless capture under a locked screen (viewed, on-token, verbatim H1); favicon.svg from the amber-prompt token + llms.txt from the brief; `.pkg`-download CTA (Releases link, not a command) and Interceptor repo everywhere; comp's stars status-line dropped per reconciled S9. interceptor.example placeholder origin, link destinations, font fallback recorded OWNER-REVIEW in build-notes.md; live 390/1440 renders + S4 quickstart output stay deferred to DOGFOOD-7's unlocked rerun. verify.sh GREEN, all authored files scan 0 FAIL.
- [ ] DOGFOOD-7: all 8 gates + renders + fix. Gate 1 conversion (already scored), Gate 2 verbatim diff, Gate 3 a11y, Gate 4 perf, Gate 5 render review + design detect (impeccable absent → degraded, recorded on gate report), Gate 6 AI-discovery, Gate 7 measurement, Gate 8 integrity (no fabricated proof). Renders at 390 (iframe-eval) + 1440 (pf-render.sh) via Interceptor. Fix every failure and re-run. Record gate report + foundry-log in loop/evidence/dogfood/. DOGFOOD-FULL is done only here.
- [ ] AUDIT: fresh-eyes audit — milestone vs plans/v3.0.md vs repo; every issue closed with evidence or reopened; TESTS.md spot-executed (pick 5 behavioral tests, walk them); verify.sh green; docs claims true. Reopen anything failing at top of backlog.
- [ ] REPORT: write plans/v3.0-overnight-report.md — everything done (by issue, with shas), every autonomous decision + justification, OWNER-REVIEW assumptions collected from the running draft, blocked/deferred items, and tomorrow's suggested test script for the owner. Commit + push.
- [ ] RELEASE-PREP: version 3.0.0 in package.json; date the CHANGELOG `v3.0.0` entry (written in 19b-2, currently marked `(unreleased)`); rebuild .skill zip; global sync preserving voice overlay (verify owner line + pattern count after); run verify.sh; ONLY if green and AUDIT passed: write loop/RELEASE-READY (runner performs npm publish + tag + gh release deterministically); then write loop/DONE with the exact promise string per Standing Rule 6.
