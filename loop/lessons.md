# Lessons (append-only; read every iteration)
# format: cause -> rule
TESTS.md fixture phrases in double quotes hit the scanner as real prose -> quote banned-phrase fixtures in backticks (code spans are scanner-exempt)
Edit old_string composed from memory missed "CTA instance" wording -> copy exact file text into old_string, never retype from recall
Quoted research prose in an evidence file tripped the banned-word scan ('arguably' inside a quotation) -> backtick any quoted source text in evidence/records, same rule as fixtures
gh issue view plain output came back empty (exit 0) through the RTK bash wrapper -> fetch issues with gh --json + explicit -R and print via python, never trust empty-but-green gh output
Axes doctrine draft named the foundry-log skeleton line before 11g builds it -> doctrine prose may not name mechanisms a later backlog item owns; describe only what exists at commit time
Two-tier rewrite left step 2 still saying "outdated exactly like missing" until a full-diff re-read caught it -> after changing a policy, grep the whole file for the old policy vocabulary before staging
Persisted cwd from a prior Bash call made `cd rel/path && write-file` skip the write while later heredocs still ran on stale files -> absolute paths for every file write in loop shell commands; never chain work after a cd that can fail
Evidence file's independent-score section drafted before the scoring agent ran (caught pre-dispatch) -> never write a result section before the run that produces it; scaffold the heading, leave the body empty until real output exists
Subagent told to "return raw data" wrote its artifact into the live .agents/ tree anyway -> dogfood subagent prompts must name the exact output path (or forbid writes); relocate strays out of the live artifact tree before commit
Em/en dashes typed into fresh evidence prose FAILed the first scan -> draft evidence files to voice rules from the start (no dashes, ranges as 'to'); the scan confirms, it does not clean up
Dedup decision comment assigned voice_scan two absorbed rules; no fix_plan item owned them and 13a-e all shipped without it -> when decomposing an issue, enumerate deliverables from its comments as well as its body and give every one a backlog owner before authoring starts
Em dash leaked into two freshly authored files in one iteration despite lesson 12 -> scan every authored md file the moment it is written, before the next edit, not at gate time
runner limit-grep missed real CLI phrasing ("You've hit your session limit · resets 3:30am") -> limit detection must include "session limit|resets [0-9]" AND a duration heuristic (fail <90s = suspected limit); never trust string-match alone against phrasings that drift
Piped detect.mjs into head and read $? as exit 0 on findings (head's status); unpiped re-run showed exit 2 -> never derive a tool's exit semantics from a piped $?; re-run unpiped (or use pipestatus) before writing doctrine
Bare source filename in prose ('optimize.md' in a provenance stamp) tripped the banned-word scan -> backtick every filename reference in prose and headings, same rule as fixtures
Walkthrough doc (2186228) shipped with no fix_plan owner or progress line; audit had to verify it after the fact -> every authored document, even reference prose, gets a backlog owner before commit; unowned commits are how claims outrun verification
Owner committed to the live checkout mid-iteration (818f585) while 12b authoring was in flight; explicit-path staging kept it out of my commits -> stage explicit file lists only, never add -A/-u; re-read fix_plan/progress from disk immediately before editing records
Scanner strips code spans before pattern regexes, so a sentence opening "No `file` exists" collapses to a bare "No ..." and fires the aphoristic-rebuttal WARN -> never open a sentence with No/Just followed by a code span; put the negation elsewhere in the sentence
Inserted a positional renumbering placeholder into TESTS.md before catching it (Standing Rule 8) -> new TESTS entries append at file end with the next number; renumbering waits for 19a, never a placeholder
Relative-path scan invocation broke on a cwd persisted from an earlier Bash call -> lesson 9 extends to invocations: start loop shell commands with cd to repo root or use absolute paths, for every command class
Three-verb-clause regex matched plural NOUNS across a parenthetical ('persists ... scores, issues ..., and opens' in fresh gate prose) -> the pattern regexes key on s-endings, not parts of speech; a comma-free span between two plural words and an 'and <verb>s' fires it, and a semicolon or period between the legs is the clean break
iteration killed mid-item leaves uncommitted skill-file edits -> next iteration misread debris as owner-live-editing and yielded; rule 9 added: verify via git log + progress.md, revert debris, redo item
Iteration authored edits for half an hour before git status revealed a killed prior iteration's uncommitted debris -> git status --short belongs in orient step 1, before picking the item; adjudicate per Rule 9 first, then author
Chained git commit after 'scan | tail' so the gate saw tail's exit 0, not the scanner's -> a scan that gates a commit runs unpiped (or checks pipestatus) in the same chain; lesson 16 extended from reading semantics to gating
Naive headless-Chrome --window-size=390 screenshot showed uniform right-edge clipping that read as page overflow; probe measured innerWidth=500 (window floor) -> narrow-viewport captures need real emulation or an iframe wrapper page, and any capture whose text clips uniformly at the right edge is suspect before the page is blamed
sips --cropOffset silently center-cropped in both flag orders on this macOS -> verify crop geometry by reading the output image, never by exit code; keep the uncropped original as the honest artifact when the crop tool misbehaves
Verified names against a head-truncated listing capture and got false MISSINGs for real skills -> never conclude absence from a truncated capture; save the full listing first, then grep the file
CORRECTED 07-18: interceptor screenshot was NOT down. It uses chrome.tabs.captureVisibleTab, which only captures the FOCUSED window's ACTIVE tab and hangs 45s when the target isn't front; the loop's own customer-research Mode-2 tabs (Reddit/localhost) were active. Fix: use loop/pf-render.sh <url> <out> (focuses Chrome, opens+switches to a fresh tab, captures, closes it) for desktop visuals; use the iframe-eval method for true-390 overflow (screenshot captures window width, not an emulated one). Never conclude 'screenshot is down' without first checking whether the target tab is active and Chrome is frontmost.
Four new evidence files used em-dash H1 title separators (`# file — desc`) and all four FAILed the first scan at once -> H1/H2 titles are prose; default to a colon (`# file: desc`), never ' — ', for the title separator; lesson 14's scan-on-write catches it but the colon default prevents it
Scanning fix_plan.md after editing it showed 43 FAILs, all pre-existing em dashes in other backlog item descriptions, not my added lines -> loop ledgers (fix_plan.md, progress.md) carry em dashes by convention and verify.sh does not gate them; scan only the lines you added (git diff +lines), never adopt a ledger's pre-existing FAILs as your item's scope
Used `beacon` as a literal analytics term in fresh prose and it FAILed the scan (it is on the AI-flagged vocabulary list) -> domain jargon is not exempt from the banned list; check scan:banned-phrases before using a technical term in prose, same scan-on-write discipline as lesson 14
Pre-build Gate 1 independent MECLABS audit scored a [TK] install-command CTA and [TK] quickstart output as absent, diverging 13pts from the builder (C=31 vs 18) on I/F/V -> a pre-build or early-rewrite independent audit systematically under-scores the factors that deferred CTA/proof elements move; record the divergence as a pre-build artifact with a re-score at the rendered gate, never 'fix' copy to close a gap the build closes (that would pressure fabricating the deferred element, the exact integrity failure the [TK] rule prevents)
Overnight screen was LOCKED (CGSSessionScreenIsLocked), so Interceptor captureVisibleTab hung 45s and pf-render.sh's Chrome-activate + AppleEvents timed out (-1712); this is NOT lesson 30's inactive-tab case -> a locked screen blocks all window-server capture; render with headless Chrome (--headless=new, temp user-data-dir, real 1440 window and a 390px iframe wrapper for true-390), the documented AUDIT-F2 fallback (real pixels, separate offscreen process); flag OWNER-REVIEW that the success gate's "via Interceptor" renders need a morning unlocked rerun. Check lock state (ioreg Root CGSSessionScreenIsLocked) before blaming the render tool.
A load animation on the hero's in-viewport PROOF (opacity 0->1, 500ms) leaves the proof invisible at first paint and races every capture -> never animate opacity on a proof-adjacency element; the earned-motion moment stays as transform-only so the proof is legible at t=0. The render check is what caught it; a paper token plan cannot.
Frozen copy snapshot (DOGFOOD-1, iter 52) went stale before the build because the real repo was renamed slop-browser->Interceptor; the old URL still 301-redirects so a resolves-or-not check passes it -> the Phase 5 "verify the repo before it ships" gate must check that the copy's CLAIMS about the repo are still true (name, install model, license), not merely that the URL returns a non-404; a redirect that resolves can still hide a renamed repo that falsifies naming/FAQ copy
headless Chrome --headless=new wrote the screenshot then hung on shutdown under a locked screen, burning the 10-min bash timeout -> the capture file is real (verify its geometry by reading the image), but the process will not always exit on its own; wrap headless-capture calls to kill the process once the output file appears rather than waiting on clean shutdown
Grepped only SKILL.md for the S5 safety controls, got zero hits, nearly flagged real features as fabricated; they were in the product README the brief also cited (product-marketing.md line 12/144) -> Gate 8 claim-tracing checks EVERY source the brief names (skill doc, repo README, docs/), not the first; a zero-hit in one cited source is not fabrication until all cited sources are checked
Platform/CI claims on a live-repo page drifted stale exactly like name/install/license did (lesson 37), but no gate names capability/platform claims, so DOGFOOD-6a's narrower reconcile missed them -> a page about a live external project re-verifies ALL factual claims at build (add platform/capability/limitations to name/install/license); filed #26
Nearly fixed a fabricated-feature Gate 8 finding in the same iteration that audited it, which would have forced a second snapshot re-freeze; deferred to the fix slice instead -> when an audit finds copy-integrity defects, record them and let ONE consolidated fix slice re-freeze the frozen snapshot against fully-corrected copy; re-freezing per-finding multiplies the Gate 2 re-diff surface and the drift risk
