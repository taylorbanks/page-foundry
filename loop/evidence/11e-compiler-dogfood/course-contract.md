# 11e micro-dogfood · compile 2 of 2 · course

FIXTURE. The product, its price, and the foundry-log excerpt below are invented for compiler testing only. This file contains proof REQUIREMENTS, never proof content; no testimonial, metric, or result appears here or may be reused anywhere. The point of this compile is to exercise the two behaviors the dev-tool compile could not: awareness-conditional job striking and a foundry-log line moving a default.

Fixture brief, in full: an 8-week cohort course teaching technical writing to working engineers. Price $349, one cohort at a time, sold in a launch window to the instructor's own newsletter list. Fixture log excerpt (format per SKILL.md Phase 6):

```
### (fixture date) · build · course-sales
- learnings: list traffic read the full curriculum section to the end; the earlier compressed page drew pre-purchase questions about scope that the long page answered on-page
```

## The five questions

1. **Conversion:** one-time enrollment in a defined offer. → `course-sales`.
2. **Relationship:** one-time; a transformation with an endpoint.
3. **Traffic:** single controlled source (launch emails to the list). Message match binds (shared constraint 8); campaign-landing pressure on nav policy, already satisfied because course-sales imports campaign-landing's CTA policy whole.
4. **Awareness state:** product-aware. The list knows the instructor and has heard the course pitched in the launch sequence. Per the archetype's entry-states block: the instructor may open the page, the arc compresses to method, results, and logistics.
5. **Decision weight:** $349 sits between the archetype's thresholds (roughly $200 focused, $500 and up long-form). The weight answer alone leaves density genuinely undecided; the log decides it below.

## Filled contract

**Goal.** Enrollment, straight to checkout.

**Buyer entry states.** Narrowed to product-aware only. Problem-aware and solution-aware framings struck: this page is unreachable except through the launch emails.

**Jobs, kept or struck.**

- Name the transformation, with timeframe and next cohort date: KEPT.
- Establish the gap: STRUCK to one sentence inside the transformation block. Product-aware readers own the gap already; the launch emails did this work.
- Name why existing paths fail: STRUCK. The archetype marks this a problem-aware job; this list has already concluded it.
- Name the method: KEPT. Mechanism before price binds regardless of awareness (shared constraint 4).
- Curriculum as outcomes, with format logistics: KEPT, and expanded (see log check).
- Establish the instructor: KEPT and moved to open the page; for this traffic the instructor is the hook, per the archetype's product-aware note.
- Student results: KEPT as the heaviest proof, immediately before the offer. If this were a first cohort, the honest substitute applies; the fixture does not decide that, the real brief must.
- Itemize the offer with payment plan: KEPT; $349 clears the archetype's payment-plan line.
- Reverse the risk with exact guarantee terms: KEPT.
- Honest urgency: KEPT; a real cohort date is legitimate urgency.
- Collect leftover objections: KEPT.
- Close with transformation restated, strongest proof, button: KEPT.

**Proof requirements.** Named student results with specific outcomes, placed immediately before the offer; instructor credibility as supporting proof only. Requirements only; the fixture supplies no proof content.

**CTA policy.** Campaign-landing's policy whole: one action, identical wording, no nav, no footer link farm.

**Ordering constraints.** The shared ten plus course-sales' three. The pruned arc (method, curriculum) still runs forward, never backward.

## Log check

The log's learnings line moves the density default. At $349 with product-aware traffic the compiler would otherwise lean compressed; the line "the earlier compressed page drew pre-purchase questions about scope that the long page answered on-page" moves the default to a full curriculum breakdown. The contract names that line as the mover, per the compiler's closing rule. That is the behavior under test: a log line changed a compiled default, and the contract says which line and why.
