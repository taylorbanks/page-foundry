# 11e micro-dogfood · compile 1 of 2 · dev-tool

Input: page-foundry itself, from this repo's README and skill frontmatter. Real product, real facts; no fixture content in this file. Compiled by walking the rewritten compiler section in `references/archetypes.md` top to bottom.

## The five questions

1. **Conversion:** install (`npx skills add` / plugin install). → `oss-project`.
2. **Relationship:** ongoing tool use, but the conversion question owns the pick; oss-project stands.
3. **Traffic:** mixed/organic (GitHub, skills.sh listing, mentions in agent-skill roundups). Nav allowed, fuller story.
4. **Awareness state:** solution-aware dominates. Readers already know agent-written pages convert poorly or read AI-made; they are comparing fixes (prompt tricks, other skills, hiring a copywriter). A product-aware minority arrives from direct mentions. No unaware traffic worth serving; nobody searches for a gated page pipeline before feeling the problem.
5. **Decision weight:** free install, minutes to try, nothing irreversible. Lightest weight the gradient has. Density: compressed.

## Filled contract

**Goal.** Install as primary (`npx skills add OWNER/page-foundry`). Star and read-docs are secondary, visually quiet.

**Buyer entry states.** Narrowed to solution-aware (dominant) and product-aware (minority). The unaware and problem-aware framings the schema allows are struck: this traffic owns its problem already.

**Jobs, kept or struck.**

- State plainly what it does and for whom: KEPT. One line, no marketing adjectives.
- Install command in hero, copy-button, badges adjacent: KEPT. The install command is the CTA for this audience.
- Zero to first useful output in fewest visible steps: KEPT. The quickstart is also the heaviest proof this page has (see proof block).
- Show how it works: KEPT, terse. Solution-aware readers evaluating trust want the gate list and the phase order, not philosophy.
- Compare honestly against alternatives: KEPT and moved early-ish for the solution-aware majority, but never before the quickstart shows the thing working (archetype ordering constraint).
- Show the project is alive: KEPT. Release cadence and changelog exist; use them.
- Link docs, repo, license, SECURITY.md at close: KEPT. SECURITY.md exists in this repo.

No job struck: oss-project's jobs are not awareness-conditional the way the persuasion-arc archetypes are. The awareness answer moved placement (comparison earlier) rather than membership. Contract records that reasoning line per the compiler's instruction.

**Proof requirements.** The brief's real proof inventory is thin by design: no star counts or named adopters may be claimed unless true at publish time. The strongest available proof is the product itself: a real run's gate report and a real built page. Per the archetype, the quickstart-with-real-output stands in front of any adoption numbers. Cut proof rather than run it thin.

**CTA policy.** Default policy; install command as primary CTA, repeated at close. Minimal nav allowed (Docs, GitHub).

**Ordering constraints.** The shared ten plus oss-project's three (install command in hero; product-aware quickstart within one scroll; comparison never precedes first sight of it working).

## Log check

No `foundry-log.md` exists for this property. Defaults stand, and the contract says so per the compiler's closing rule.
