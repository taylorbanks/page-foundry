# 17b micro-dogfood · compile the DESIGN.md projection and lint it

Input: the page-foundry docs homepage as the property (locked regime, same property as the 17a dogfood), with every source real and on disk before compilation: `docs/theme.css` (token values and the "Slab" direction comment), the 11e compiled contract (`loop/evidence/11e-compiler-dogfood/dev-tool-contract.md`, the Overview's goal and audience), and the anti-slop list in `references/design-direction.md` (carried verbatim into Do's and Don'ts). Output: `DESIGN.md` beside this file, compiled by walking the generation-rules table in the rewritten `references/handoff.md` row by row. Package files `00` to `03` do not exist for this property, so the record maps each table row to the artifact that would feed it (`theme.css` and its direction comment stand in for `03`; the compiled contract stands in for `00`); no value was invented to fill the difference.

What the compile and lint exercised, and what it proved:

1. **Every line traces.** Frontmatter tokens are `docs/theme.css` values byte-for-byte, under the property's own names; the accent also landed as `primary` per the rule, and the four components carry the real button and command-bar treatments. The 5.9:1 contrast figure in the Colors prose is the claim `theme.css` itself states, and the linter's contrast rule (which converts and checks fg/bg pairs on components) raised no finding against the black-on-red pairs.
2. **The eight-section order lints clean.** `npx @google/design.md lint` (v0.3.0): exit 0, 0 errors, 5 warnings, 2 infos; verbatim output in `lint-output.json`. No section-order finding, so the published eight-section order as the doctrine states it is the order the linter accepts.
3. **The expected warning class arrived and was dispositioned, not suppressed.** All 5 warnings are orphaned-token findings (`red`, `ink-2`, `panel`, `wash`, `dim` defined but unreferenced by components). Doctrine disposition applies: these tokens exist for the consuming tool's use, not for component cross-reference; a projection that deleted them to silence the linter would hand the tool less than the property has.
4. **The blocking claim is verified live, not assumed.** A probe copy with `{colors.primary}` rewritten to `{colors.nonexistent}` exits 1 with 2 error-severity findings ("Reference {colors.nonexistent} does not resolve to any defined token."); verbatim output in `lint-broken-ref-probe.json`. So the doctrine line "`broken-ref` blocks delivery" rests on observed exit semantics, unpiped.
5. **One real cost surfaced and is recorded rather than papered over.** The linter's info line "No 'spacing' section defined. Layout spacing will fall back to agent defaults" is the price of the deterministic rule: `theme.css` expresses padding as `clamp()` formulas, which are not valid spacing Dimension tokens, so the spacing rhythm rides in the Layout prose instead (prose-primary is the format's own philosophy). A consumer that reads only frontmatter gets defaults; one that reads the file gets the rhythm.
6. **The projection passes the same voice gate as the package prose.** `voice_scan.py`: PASS, exit 0, zero WARNs.
