# REVIEW-4 · fresh-eyes audit of WS4 (iters 25 to 26, plus one unowned commit)

Scope: every commit in `v3-night-3..HEAD` at audit time: `193dbdf` (records backfill), `2186228` (orchestration walkthrough, no backlog owner), `e745c29` (records), `872c238` (17a), `fe62776` (17b), `61fb7f1` (records). Audited against issue #17's body and closing evidence comment, `plans/v3.0.md` Workstream 4, `plans/research/pf-handoff.md` and `plans/research/stitch-format.md`, and the loop's standing rules. The author iterations were 25 and 26; this audit is a different context with no stake in their conclusions.

## Verdict: GREEN. Zero reopens. Tag `v3-night-4`.

## What was checked, and how

**17a, manifest and template (`872c238`).** `references/handoff.md` read in full against each issue deliverable: manifest `00` to `06` plus `assets/` all present with per-file doctrine; the five non-negotiables and the creative-license grant sit in `00` as verbatim carry blocks; `01` image slots carry display dimensions and aspect ratios plus the `assets/manifest.md` geometry row spec; `04` fills from a fixed template whose IDs map to ship gates by prefix, G6 and G7 included; `05` scopes the voice excerpt to tool-generated strings; `06` carries the return log spec and a fixed revision-request shape with full-package re-attachment reasoned from the no-session-memory constraint. The fabrication ban in non-negotiable 4 was diffed against `references/ship-gates.md` line 76: the quoted sentence is byte-identical to the integrity gate's first check. Gate numbering uses integrity as Gate 8 throughout. `SKILL.md` Phase 5 handoff paragraph enumerates the new manifest and keeps the run open until post-build gates pass.

**17b, projection and packaging (`fe62776`).** The generation-rules table names eight body sections; their names and order were checked against the published-spec section list in `plans/research/stitch-format.md` and match exactly, `version: alpha` pinned, with the six-versus-eight question deferred to the Phase 4 step 6 ruling, which exists in `SKILL.md` and says what the handoff text claims it says. The property-root versus package `DESIGN.md` collision is disambiguated as input versus output. Propose-then-lock frontmatter carries name and version only, matching the OWNER-REVIEW entry in the report draft. The per-tool table's three rows match the issue deliverable, and the single-prompt row prices its seam as a styled draft that re-enters Phase 5, which is the second OWNER-REVIEW entry. Both entries are present in `plans/v3.0-overnight-report.md`.

**Independent reproduction of the lint claims.** Not taken from the evidence files: this audit re-ran `npx @google/design.md lint` (0.3.0) against the committed `loop/evidence/17b-projection/DESIGN.md`. Result: exit 0, 0 errors, 5 warnings, 2 infos, and the finding list is identical to `lint-output.json` down to the token names. The broken-ref probe (`lint-broken-ref-probe.json`) shows 2 error-severity findings and the author's record states the unpiped exit was 1, consistent with lesson 16's rule. The doctrine line that a `broken-ref` finding blocks delivery rests on observed behavior twice over.

**Micro-dogfood evidence.** `loop/evidence/17a-handoff/04-acceptance-criteria.md` was walked against the template block: IDs byte-identical, slots filled from live `docs/` values, seven N/A marks each with a reason on the line, and the G7 fills record the property's missing measurement plan as an owner open item instead of absorbing it, which is the behavior the fixed template exists to force. `loop/evidence/17b-projection/record.md` claims every frontmatter value traces to `docs/theme.css`; spot-checked and consistent, including the spacing-fallback cost recorded as a real cost.

**The unowned commit (`2186228`).** `plans/orchestration-walkthrough.md` shipped with no fix_plan owner and no progress line, five minutes before 17a. Its factual claims were verified at HEAD: the seven-core-required policy matches 13a; the data spine matches 13b's artifact directory; the gate list matches `references/ship-gates.md`; the answer-block word range matches `SKILL.md` and Gate 6; the named pattern tells (negative parallelism, three parallel verb-clauses) exist as regexes in `references/voice.md`. The one number worth distrust, 46 detector rules, was independently counted in the pinned impeccable clone's `registry/antipatterns.mjs`: exactly 46 entries, which also settles the uncounted-flourish flag `plans/research/critique.md` raised against that figure. At commit time the walkthrough's handoff sentence described 17a/17b content that had not landed yet; both landed within fifteen minutes, so nothing at HEAD claims anything unbuilt. Process drift recorded in observations, content clean.

**Mechanical gates.** `bash loop/verify.sh`: GREEN, exit 0. `voice_scan.py` across every markdown file the range touched: 0 FAIL; 4 WARNs, all on `SKILL.md` lines 8, 167, 222, and 261, all pre-existing doctrine WARNs on lines this range never edited (REVIEW-1 and REVIEW-2 dispositions stand). `plans/orchestration-walkthrough.md` and both evidence records PASS clean. Working tree clean; no forbidden files tracked; open milestone issues (#12, #18, #19, #21, #22) match the remaining backlog one for one; #17 closed with its evidence comment.

## Observations (no reopens)

1. The criteria template's G5 heading reads `G5 · Design` while `references/ship-gates.md` names Gate 5 `Render review`. The prefix-to-number mapping stays mechanical and G5.1 to G5.3 map to Gate 5's actual checks, so nothing breaks; the label should converge when 18 (#18) touches the gate texts or 19a sweeps TESTS. Cosmetic, worth one line of attention, not a reopen.
2. The 17a fill replaces an inapplicable criterion's body text with its N/A reason (G3.8, G3.9) while the record says nothing was deleted. The template's own instruction says mark N/A with the reason on the line, and IDs stay stable, so the read is defensible; DOGFOOD-FULL should keep an eye on whether builders miss the dropped body text.
3. WS4 shipped no TESTS.md entries. That is by design: 19a owns the full regression sweep and its item text already names the handoff manifest and projection; this audit confirms 19a must also cover the per-tool seam rule and the projection lint disposition doctrine.
4. `2186228` shipped outside the backlog. The content survived audit, but unowned commits are how claims outrun verification; lesson appended so authoring iterations route new documents through fix_plan first.
