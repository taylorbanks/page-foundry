# AUDIT-F1a micro-dogfood · per-job shapes in the compiler output, Phase 4 demoted to confirm-or-override

Input: the 11e dev-tool compile (`loop/evidence/11e-compiler-dogfood/dev-tool-contract.md`), page-foundry itself as the product. Real product, real proof inventory; the same brief facts, recompiled through the new closing block of the compiler in `references/archetypes.md`. Then a static walk of the rewritten Phase 4 step 3 against the compiled set. This is a doctrine walk on a real artifact; no Phase 4 has run for this property and this file does not pretend one has.

## The new compiler close: a shape per kept job

The 11e contract kept seven jobs. Two of them resolve outside the lexicon and the block says so: "state plainly what it does and for whom" and "install command in hero" are the hero, and the hero's form is the hero-form composition axis (already recommended in the axis block), so they take no lexicon line. The remaining jobs each get a shape and a reasoning line naming the input that chose it:

- **Zero to first useful output (quickstart)** → numbered steps. The mechanism's real order chose it: install, invoke, first gate output is a genuine sequence, the lexicon's own condition for the shape.
- **Show how it works (the gated pipeline)** → architecture diagram for technical readers. The audience chose it: practitioners evaluating trust want the phase order and the gates at a glance, and the quickstart already holds the numbered-steps shape, so a second stepped section would flatten into one wireframe.
- **Compare honestly against alternatives** → comparison table. The objection map's leftovers chose it: the solution-aware majority is weighing this against prompt tricks and other skills, and a table is the form that comparison honestly takes.
- **Show the project is alive** → artifact screenshot. The proof inventory chose it: no adoption numbers may be claimed, and the strongest real proof is the product's own output, a real run's gate report shown as the artifact it is.
- **Link docs, repo, license, SECURITY.md at close** → one line restating the outcome, with the button. The decision weight chose it: lightest weight on the gradient, compressed density, so the close restates and repeats the install command without a second persuasion pass.

Lexicon membership check, run mechanically against the section-shape lexicon in `references/archetypes.md`: numbered steps (how-it-works list), architecture diagram (how-it-works list), comparison table (objections list), artifact screenshot (proof list), one-line restatement with the button (close list). Five of five recommended shapes are sanctioned entries; zero off-lexicon shapes, so nothing needs the record-what-and-why clause.

## Static walk of Phase 4 step 3 (confirm or override)

The rewritten step opens on a spec that arrived with confirmed shapes, which is exactly what the block above produces once sign-off fixes it. Walking the step's demands against this artifact:

1. **Confirm each one**: every shape above carries a reasoning line naming its input, so a Phase 4 confirming it has something to confirm against. Satisfiable from the artifact alone.
2. **Override where the direction demands a different form**: the recorded-decision requirement has a concrete target. Example from this set, stated structurally: a token direction whose density cannot carry a full comparison table would override that section to the inline-callout objections shape. The spec would record the override and its reason, and the comparison section's copy, written as table cells, changes obligations, so it re-enters Phase 3 step 5 and the snapshot moves with it. The step's text names both requirements; the artifact gives the override a reasoning line to argue against.
3. **Re-deriving from scratch is a defect**: with the shape block in the contract, a from-scratch Phase 4 pick is now detectable as drift between the spec's shapes and the built page, which is what test 54's failure line checks.

## What this proves

- The compiler's closing block produces per-job shape recommendations with one reasoning line each, on a real compile, with hero-form jobs correctly deferred to the axis block rather than double-assigned.
- Every recommended shape resolves to a sanctioned lexicon entry, checked mechanically.
- The demoted Phase 4 step is walkable against the compiled artifact: confirmation has material, an override has a recorded-decision path with the re-trigger consequence stated, and from-scratch re-derivation is detectable as spec drift.
