# 12b micro-dogfood · docs-dev-tool-landing compile

FIXTURE. The product below is invented for compiler testing only. This file contains proof REQUIREMENTS, never proof content; no adopter, metric, or code output appears here or may be reused anywhere. The point of this compile is to exercise the new question-1 mapping and the contract's boundaries against `oss-project` and `saas-homepage`, the two archetypes that previously absorbed this brief badly.

Fixture brief, in full: a hosted log-search API for engineers, closed source, key-gated. Free tier: a stated ingest cap, no card. Paid: usage-metered with published rates and an enterprise tier behind a sales conversation. Traffic: search for the problem category, plus arrivals from an SDK's docs. Audience: the engineer who will integrate it; a manager approves the invoice above a team threshold. No `foundry-log.md` exists for this property.

## The five questions

1. **Conversion:** a practitioner's first successful call on a commercial developer tool. → `docs-dev-tool-landing`, per the compiler's new question-1 mapping. Not `oss-project`: nothing installs from a repo, and the quickstart is key-gated behind signup. Not `saas-homepage`: the buyer who decides is the practitioner who integrates, and the page is measured on time-to-hello-world, not trial starts.
2. **Relationship:** ongoing, usage-metered; the will-it-last job (versioning, deprecation policy) is load-bearing for a reader adopting a dependency.
3. **Traffic:** mixed organic (category search, docs referrals); no single source, so message match binds only loosely and navigation is standard, docs-forward.
4. **Awareness state:** solution-aware dominates; the searcher owns the problem and is comparing approaches. Docs-referral arrivals are product-aware and reach for the quickstart.
5. **Decision weight:** free to start, invoice later. Light to enter, real to keep; density: compressed, with the trust material (architecture, data handling, deprecation policy) present and never leading.

## Filled contract

**Goal.** First successful call: key issued, quickstart run, real query output seen.

**Buyer entry states.** Both of the contract's states arrive, per the brief. The economic buyer exists (invoice above a team threshold), so the security-and-SLA material is KEPT and placed off the leading path.

**Jobs, kept or struck.**

- One-sentence fit statement, no marketing adjectives: KEPT.
- Working code in the first scroll, real request and response: KEPT; the fixture supplies none, per the fabrication rule.
- Quickstart from key to first call, free-tier terms stated: KEPT.
- Docs one click from everywhere, starting in the hero region: KEPT.
- How-it-works trust section: KEPT; data handling is a standing objection for a log product.
- Pricing disclosure: KEPT; published usage rates plus the free-tier cap. The enterprise sales route stays quiet beside self-serve.
- Will-it-last: KEPT; deprecation policy and production tenure.
- Closing repeat of the key-and-quickstart action: KEPT.

**Proof requirements.** Working call as heaviest proof; docs link as the taste-it move; named adopters only when real. Requirements only; the fixture supplies no proof content.

**CTA policy.** Get-a-key as primary; navigation standard and docs-forward; sales route visually quiet.

**Ordering constraints.** The shared ten, plus the archetype's three: code in the first scroll, pricing before the final ask, sales route never before the self-serve path.

**Axes.** Narrative shape: demo-led; solution-aware traffic starts at the mechanism, and here the code is the demonstration. Hero form: product-in-hero, where the product visual is the code block. Proof strategy: interleaved; the inventory is many small pieces riding beside claims. Density: compressed; decision weight chose it in question 5.

**Log.** No `foundry-log.md` exists for this property, so the axis defaults stand, and the contract says so.

**Post-conversion.** The first-call moment lands in the product's own console, which the platform owns; the spec names it (shared post-conversion obligation) and no thank-you page is built.

## What this compile proves

Question 1's new mapping routes the key-gated commercial dev tool away from `oss-project` and `saas-homepage` with stated reasons; the six blocks fill from the brief without inventing content; Gate 6's public-pricing enumeration now covers this archetype, matching the contract's pricing-disclosure job.
