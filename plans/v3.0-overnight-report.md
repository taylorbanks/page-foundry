# v3.0 overnight report

Status: IN PROGRESS (the loop appends as it works; final synthesis happens in the REPORT item).
Started: 2026-07-17 evening · Runner: loop/run.sh · Backlog: loop/fix_plan.md

## Work completed
(appended per item: issue, what shipped, commit shas, evidence)

## Autonomous decisions and justifications
(appended when made, not reconstructed)

- **11g: anti-template check window set to the last three runs.** The plan says "last N pages" without picking N. Chose three: one repeat can be a coincidence of similar buyers, and a longer window would flag pages whose old skeletons predate the current contract set. The check is cheap either way (it reads three log lines), and the resolution is a justification in the spec, not a forced rebuild, so a too-tight N costs a sentence, not a page. Worth a morning glance if the owner wants a different window.
- **13a: core-tier hard stop applies to missing, not outdated.** The owner decision says preflight "stops until installed"; an outdated core companion is installed and still runs as the primary path, so declining its update proceeds (with the version gap noted), consistent with TESTS.md #10 ("outdated is offered, not forced"). Treating outdated-core as a hard stop would have contradicted an existing test to enforce a reading the decision text does not require.
- **13a: skill-package README trued immediately, root README deferred to 19b.** The two-tier policy made the skill README's "all optional" line false, and that file ships inside the package, so it was corrected in the same commit. The root README's "Built on" credit to theme-factory and its "all of these are optional" sentence are also now stale, but that file is repo marketing, owned by the 19b docs truth pass; deferred there rather than expanding 13a.
- **13b: the brief does not move into the run artifact directory.** The backlog line says Phase 0 "adopts v2.x artifacts (old .agents/product-marketing.md, foundry-log)", which could read as relocating the brief into `.agents/foundry/<product>/`. Chosen reading: the brief stays at `.agents/product-marketing.md` in the repo case, because that path is the marketingskills convention every core companion reads (product-marketing's own doc says all other skills reference it there); moving it would quietly break the core-tier orchestration 13a made mandatory. Only no-repo products keep the brief inside the foundry directory, replacing v2.x's vaguer "beside the page output". Adoption of the brief therefore means finding it where v2.x left it and confirming its v3 home, not relocating it. Worth a morning glance since the plan's directory listing is ambiguous on this.
- **13d: "frontend-design always produces 2-3 token plans" scoped to when frontend-design is the token source.** The research note says "always (not just explore mode)", but the token source priority puts brand assets and a committed gstack DESIGN.md ahead of frontend-design, and generating three competing palettes for a property whose theme is already committed would contradict "one property, one theme, many pages." Chosen reading: whenever frontend-design is the one producing the plan (no brand tokens, no committed theme), it produces 2 or 3, in every mode. Literal-always would have made Phase 4 argue with its own source-priority list.
- **13d: the independent score is the score of record, and the no-subagent fallback is an honest self-score, not a blocked gate.** The plan says only "record divergence"; the gate now also says the cold score governs (flagged factors are judged on it) and that an environment with no way to open a fresh context runs on the self-score with the non-independence stated in the report. Alternative was failing Gate 1 outright in single-context environments, which would make the skill unusable exactly where most users run it. Dogfood measured a real divergence of 2 (self 28, cold 26) on the shipped homepage, driven by the incentive factor; the mechanism finds real signal.
- **11d: newsletter-capture gained a hero-proof mandate the v2.x file never had.** The v2.x numbered version (form in hero at slot 1, proof at slot 3) violated rule 8 / shared constraint 7 (mobile reader hits proof before any form) on every mobile render; the contradiction predates v3 but the contract conversion forced a resolution. Chosen fix: the contract's first ordering constraint requires a proof element in the same hero viewport as the form (creditable count, reader quote, or taste-it link beside the field), which satisfies shared constraints 2 and 7 instead of silently subtracting from the shared set (which the schema forbids). The alternative, carving a newsletter exception out of shared constraint 7, would have re-legalized the exact form-before-proof pattern rule 8 exists to prevent. Judgment call worth a morning glance: it adds a small design obligation to every newsletter hero.

- **13f: impeccable's page-wide aphorism threshold became voice.md doctrine, not scanner code.** The source rule fires once at 3+ instances per page; voice_scan patterns WARN per instance for human review. Adding counting logic to the script for a WARN-level rule contradicted the item's own preference for line regexes, so the scanner surfaces every instance and the voice rules carry the three-per-page rewrite line for Phase 3 to apply. Strictly more surface than the source rule, same judgment. The kicker check did need script logic (cross-element structure); its computed-style gates were adapted to source-level tells (all-caps text, uppercase or eyebrow/kicker classes) since a stdlib scanner has no CSS cascade, with the limit named in the docstring. Absorption done from a pinned clone of impeccable 3.2.1 at `8967edc`, kept at `~/projects/impeccable` for WS3's detect and craft-rule items.
- **13f: three pre-existing doctrine sentences now WARN under the absorbed rebuttal pattern and were left standing.** archetypes.md (2) and ship-gates.md (1), each a real sentence-then-`No ...` construction below the three-per-page line, none in shipped page copy. Rewriting doctrine prose to dodge a WARN the same commit introduced would be gaming the gate; the WARN-by-design policy from REVIEW-1/2 covers them.

- **15a: the issue's optional-invocations deliverable shipped here, not in 15b.** The WS3 decomposition gave 15a the table entry, the PRODUCT.md handoff, the hooks note, and the SECURITY paragraph, and gave 15b the Phase 4 wiring and detect gate; the issue body's sixth deliverable (critique advisory with MECLABS as arbiter, live in explore, overdrive rare, hooks off) had no backlog owner, which is the exact gap the lesson-13 rule exists to catch. It is invocation doctrine rather than phase wiring, so it landed in 15a's integration notes. 15b still owns `document`, `palette.mjs`, and `detect.mjs`.
- **15a: the companion row names only the advisory commands until 15b lands.** The enhancer row lists `critique`, `live`, `overdrive`, and the refinement verbs; `document` and the detect gate enter the row when 15b wires Phase 4 and Gate 5, so shipped text never names a mechanism a later item owns (the same incremental pattern as 11a's still-numbered archetype bodies). Correction recorded in the 15a evidence: the pinned impeccable clone at `8967edc` carries frontmatter version 3.9.1; the 13f note's "3.2.1" misstated it.

- **15b: the accepted-findings file is the product's `.impeccable/config.json`, not a page-foundry-side file.** The plan reads "per-property accepted-findings file ... mirroring `.impeccable/config.json` ignores". Verified in the pinned clone that `detector.ignoreValues` entries natively carry a `reason` field and that `detect.mjs` honors the file. A parallel page-foundry file would be a second record the detector never reads, the two-competing-docs failure the 15a singleton rule exists to prevent, so the doctrine writes acceptances into the product's config with a mandatory `reason` and treats a reasonless ignore as a suppressed finding, not an accepted one.
- **15b: DESIGN.md targets the published eight-section spec while accepting impeccable's six-section output.** The owner decision pins the published spec; impeccable's `document` reference writes six sections and forbids a Layout section. A six-section file is legal under the published spec (sections are omittable, never reordered), so Phase 4 accepts what `document` writes, adds `## Layout` and `## Shapes` only when the token plan holds real content for them, pins `version: alpha`, and never strips sections from a richer file.
- **15b: detect exit semantics verified empirically before any doctrine text.** Clean input prints `[]` and exits 0; findings print a JSON array and exit 2. An earlier check misread the findings case as exit 0 because `$?` was read after a pipe into `head`; caught on the unpiped re-run, lesson recorded. The micro-dogfood also surfaced two true findings on our own docs page (numbered section markers, side-tab accent), filed as #22 ahead of the 19b docs pass rather than silently expanding 15b.

- **16: the squint test is absorbed as design doctrine; its render-gate checklist line stays with #18.** Issue #16's rule list says "squint test in render gate", but the gate-upgrades consolidation (#18) owns every gate-text change, and doctrine may not name a mechanism a later item builds. design-direction.md states the test itself (blurred page, primary/secondary/groupings identifiable in 2 seconds, applied hero-first then per section); #18 adds the Gate 5 checklist line. Same boundary pattern as 15a's companion row.
- **16: voice.md's stale "impeccable 3.2.1" provenance corrected inside this commit.** The 15a note above already records the misstatement (clone `8967edc` is frontmatter 3.9.1); leaving the known-wrong version in voice.md while stamping design-direction.md with 3.9.1 would ship two provenance claims that contradict each other about the same clone. One-line fix, evidenced in the 16 spot-check notes, rather than a separate backlog item.

- **17a: the title and meta description became labeled entries in `01-copy.md`.** Gate 6 already holds them to the voice rules ("written to the same standard as page copy"), but in handoff mode nothing carried them to the tool, so the tool would write them post-voice-gate, the exact microcopy gap the package rewrite exists to close. Criterion G6.5 now checks the build against the copy file's entries. Build mode is untouched; its title and meta were always the builder's own Phase 5 output under Gate 6.
- **17a: round-trip ownership defined in `06-return-spec.md`.** The research flagged it undefined (patching the returned HTML forks the source of truth from the tool's project). Chosen rule: revisions go to the tool and its project stays the markup's source of truth; when the owner patches returned files directly instead, the patched files become the build of record for later rounds, and a patch that touches copy re-enters Phase 3 step 5 like any post-pass edit. The alternative (page-foundry silently patching) recreates the drifted-build failure the verbatim diff exists to catch.
- **17a: "do not add, merge, or reorder sections" narrowed to content order; visual sectioning licensed.** Per the research finding that the old rule conflated the message contract with visual layout, non-negotiable 2 binds the reading order and each claim-to-proof pairing while explicitly licensing visual merges and splits. Relatedly, the candidate skeletons and their pre-reads stay out of the package's `02-page-spec.md`: they are decision history, and shipping rejected orderings to a design tool invites it to relitigate the pick.

- **12b: docs-dev-tool-landing joined both public-pricing enumerations.** The contract carries a pricing-disclosure job (the research line: pricing coexists with docs-first credibility), and an archetype with public pricing missing from Gate 6's enumeration would make the agentic-buyer `/pricing.md` check silently skip it. conversion-rules' pricing-block list gained it too, plus paid event-webinar tickets (free registration stays out; the offer jobs only join on the paid merge). Same extension pattern 12a applied for pricing-page.
- **12b: newsletter-capture's question-1 mapping sharpened to "recurring publication that exists".** The bare "Email address" mapping became ambiguous the moment waitlist-coming-soon entered the compiler (both conversions are an email). The added words state the boundary the two contracts' notes define: newsletter sells a thing that can be tasted, waitlist sells access to one that cannot yet. The waitlist compile fixture exercises the boundary through the build-in-public merge case.
- **12c: ecommerce-product's machine-readable price rides the `Product` schema, not `/pricing.md`.** The site-root `/pricing.md` convention serves one pricing model per property; a store has a price per product page, so the agentic-buyer surface for this archetype is the `Product` JSON-LD (template added to Gate 6, schema price required to match the visible buy box). ecommerce-product therefore did NOT join Gate 6's `/pricing.md` enumeration; it DID join the pricing-psychology enumeration, with a qualifier that a buy box applies the levers that fit its shape (no tiers to guide; anchoring, framing, and endings still bind).
- **12c: changelog-launch-post gets no dedicated JSON-LD template.** Its pages are announcements on the product's site; the existing homepage defaults (`Organization`, `WebSite`) plus the FAQ template where one applies cover it, and inventing an `Article` template would add gate machinery no acceptance line asked for. Stated here so the choice is visible rather than silent.
- **13h: confidence and frequency landed per theme, not per quote.** The backlog line compresses audit finding F5 to "synthesis fields per quote (theme, confidence label, date, frequency-x-intensity note)", but the pinned companion defines confidence over sources-per-theme (High needs three or more independent sources), which no single quote can satisfy; a per-quote confidence label would be a field with no criteria behind it. Shipped placement: theme tag and date on each Verbatim attribution line, confidence and frequency-times-intensity on the ranked Themes section, matching the companion's own synthesis template. The dogfood also applied its "independent sources" criterion as authors-not-comments (one prolific author counted once), which the doctrine inherits by deferring to the companion's criteria rather than restating them.
- **12c: test 38's enumeration synced to the reference files.** 12a and 12b extended the Gate 6 and conversion-rules public-pricing enumerations but left the TESTS.md entry naming only the original three archetypes; the fix_plan line owned the sync and 12c performed it (pricing-page, docs-dev-tool-landing added to the test's enumeration).
- **13m: the fallback brief template gained the two consumed sections.** The item scopes invocation text, but doctrine that reads the brief's Switching Dynamics and anti-persona sections half-breaks on a degraded Phase 0 whose template never collects them; `assets/brief-template.md` now carries both, mirroring product-marketing 2.3.0's sections 7 and 8, which also tightens the template's stated compatibility claim. Two sections, no new fields beyond the companion's own shape.
- **13m: Gate 1's red-team line aligned with the Phase 3 walk.** Phase 3 says the same check re-runs at the gates; leaving the gate's reader set narrower (no forces, no anti-persona reader) would make the re-run a different check, the doctrine-drift failure lesson 8 records at the vocabulary level. The gate line now names the forces and the anti-persona reader in the phase text's terms.
- **13n: the site-architecture companion-table row shipped with the ordering fix, ahead of 21a.** The backlog folds site-architecture's full catalog contract into 21a, but Phase 2 step 1 now names the invocation, and the orchestration doctrine says every phase-named companion is invoked per its table contract; shipping the phase text without the row would leave the registry and the doctrine disagreeing for several iterations, the exact between-workstreams gap REVIEW-2 reopened #13 for. The row is enhancer tier with the F12 fields; 21a keeps the scope-escalation seam and the catalog cross-verification. Tier call is also autonomous: the audit named no tier, and core-tier would hard-stop every single-page run for a companion only multi-page properties consume, so enhancer with a stated degraded path (builder answers the site questions from the brief) fits the two-tier rationale.
- **13n: Phase 2 step 2's archetype enumeration synced (14 to 17).** 12c added `agency-services`, `ecommerce-product`, and `changelog-launch-post` to the compiler's list in `references/archetypes.md` but SKILL.md's Phase 2 enumeration was never updated; the 13n rewrite touched exactly that sentence, so the sync rode along, recorded in the commit message rather than silently. Same class as 12c's test-38 enumeration sync; noted for 19a's regression sweep to add an enumeration-consistency check between the two files.
- **13o: the critique-to-polish wiring carries a contract-deferral rule the audit did not spell out.** The audit's fix was one sentence (a failed render review runs `polish` against the critique snapshot); shipping only that would hand a Nielsen-driven refinement pass write access to a conversion page with no stated boundary, contradicting the settled MECLABS-arbiter decision at the exact moment polish acts on Nielsen findings. The new Gate 5 line therefore adds: a polish move colliding with a conversion requirement defers to the conversion rules with the deferral recorded like an accepted finding, and copy stays out of polish's hands (Gate 2's verbatim diff is the tripwire). Both extensions restate existing settled doctrine at the new seam rather than inventing policy; recorded here because the audit text named neither.
- **13p: OG-card production is companion-free.** The card is typography over `theme.css` tokens carrying snapshot copy, captured with the render tooling Gate 5 already names; no image or canvas companion enters the doctrine, because 21b owns the asset-ownership split and pre-wiring a companion here would decide 21b's question early. When 21b ships, richer art paths can layer on top; the token card is the floor. Two more calls inside the same bullet: the card source `og.html` lives in the run artifact directory rather than `pages/` (the deployables-only rule; only the capture ships), and the no-render-tool-no-brand-image case asks the user for a source image and omits `og:image` until one lands, on the reasoning that a dangling meta tag breaks every share preview while an absent one merely forgoes the card.
- **F1a: hero-form jobs stay on the composition axis, outside the per-job shape block.** The audit's fix said a shape per job; the lexicon has no hero category because hero form is already one of the four axes, so jobs that resolve to the hero (positioning line, install-command CTA) take no lexicon line and the compiler close says so. Double-assigning them would let an axis setting and a shape line disagree about the same element. Also F1a's evidence trail lands on tracking #20: the v3.1 audit findings got their own issues (#23, #24, #25) but the owner-approved v3.0 set (F1a/F1b/F2) has none, and #11/#15, whose territory F1 touches, are closed with their own evidence chains.
- **13p (Rule 9 debris): the killed prior attempt's handoff.md edit was reverted, and its design intentionally not adopted.** That edit had this skill produce the OG card and favicon pre-packaging in handoff mode, arguing the inputs all exist pre-handoff. They do not in the propose-then-lock regime, where the tool proposes the token system the card would be styled from; and in the locked regime, criteria G6.6/G6.7 already make both assets acceptance criteria the tool must meet, inside its own art direction. The shipped doctrine keeps production with the tool in handoff mode, both regimes.

- **F1b: Phase 3 now ends at `copy/approved-draft.md`, a new named artifact.** The audit item moves the freeze to the copy-fit pass but names no carrier for Phase 3's approved-but-unfrozen state; a run interrupted between Phase 3 and the pass needs that state on disk under a known name (the artifact-directory rule: phases hand work through files, never context). The name says exactly what the file is: approved, still a draft because no form has seen it. Adoption note: v2.x `copy-approved.md` migration in Phase 0 step 2 is untouched, since old runs froze at Phase 3 and their snapshot remains their snapshot.
- **F1b: Phase 4 step 3's override copy-work now rides the copy-fit pass.** F1a's text said override-affected copy "goes back through Phase 3 step 5, and the snapshot moves with it", written when the snapshot existed by Phase 4. With the freeze moved after Phase 4, there is no snapshot to move at override time, so the re-work lands in the pass immediately downstream and its planned re-entry covers the voice chain. One planned trip instead of two unplanned ones; test 54's clause synced.
- **F1b: handoff propose-then-lock reduces the pass to the freeze.** The item text does not address handoff; under a locked direction the pass runs normally, but under propose-then-lock no form exists to fit against, and fitting against a form the tool has not proposed would be guesswork. The pass writes the snapshot and fit lands with the returned build's revision rounds, which `references/handoff.md` already owns. Stated in the section and in test 55.

- **F2: the comp stage is build-mode only (an explore winner's build included); handoff mode is excluded by structure.** The item text names no mode scoping. In handoff mode this skill never builds the page, and the returned build's bounded revision rounds in `references/handoff.md` are already the comp-equivalent loop, owned by the tool that owns the markup. Running a comp stage there would have page-foundry rendering a page it is contractually not building. Gate 5's new comp-record line reads N/A in handoff mode with that reason.
- **F2: the bounce is capped at one, then the user decides.** The audit granted bounce-to-Phase-4 authority without bounding repeat bounces, and the order-redteam's own requirement is that loops be designed and bounded. Shipped rule: a bounced direction gets fresh rounds once; a second bounce means the direction's inputs are wrong and goes to the user with both records. Unbounded re-derivation is the iterate-to-taste failure the two-round ceiling exists to prevent, applied at the next level up.
- **F2: a direction that arrived without a scene sentence and anchors gets them written at comp entry.** Supplied-theme runs collapse Phase 4, so the brief-discipline artifacts the critique judges against may not exist; the stage writes them from the theme and the brief before the first round, because a critique with no reference is taste. The dogfood exercised exactly this branch on the docs property.
- **F2: the comp renders the approved draft, and Gate 2 never diffs it.** The comp precedes the freeze by design (the fit pass needs the render), so its copy custody stays with the copy-fit pass and Phase 5; the integrity rules still bind the comp whole since its content becomes the page. The comp lives in `comp/` in the run directory under the deployables-only rule, and Phase 5 starts from the revised comp rather than rebuilding composition the rounds settled.
- **F2: Gate 5's screenshot line gained a narrow-viewport capture warning, a small scope addition backed by dogfood evidence.** The walk found that headless Chrome (`headless=new`) floors its window width at 500 (probe: `innerW=500` under `--window-size=390`), so a naive 390 screenshot is a 500px layout cropped to 390 whose uniform right-edge clipping fabricates overflow findings. The gate line now requires true viewport emulation or an iframe wrapper page for the 390 capture. Without the warning, the skill's own render gate can hallucinate a defect or, worse, crop away a real one.

## OWNER-REVIEW: assumption-based decisions
- Runner design: --dangerously-skip-permissions on this machine, bounded per run.sh header; release is deterministic-bash-only, gated on RELEASE-READY + DONE + verify.sh.
- Continuous small pushes to main (no worktree); v3-night-N tags are rollback points.
- 06:30 local wall clock; ship-what-is-green if the milestone outsizes the night.
- 13f overlay gap: the owner's `taylor-voice.md` overlay replaces `references/voice.md` wholesale, so the two new aphoristic pattern lines will not reach the owner's installed copy until the overlay gains them (the kicker check ships in the script and applies regardless). Morning action: append the two `scan:patterns` lines from the 13f commit to `taylor-voice.md`.
- 17b single-prompt seam: the plan's per-tool line says single-prompt tools "get design.md" and no more. I priced that seam explicitly: because the projection carries no copy, criteria, or return spec, handoff.md now says a single-prompt tool's output is a styled draft, never a build of record, and reaching a shippable page re-enters Phase 5 with every gate. That demotion is my inference from the plan's own verbatim-copy and gate doctrine, not stated in the plan; if you intended single-prompt tools as a full delivery channel, the middle row of the per-tool table is the text to revisit.
- 17b propose-then-lock projection: under propose-then-lock the generated `DESIGN.md` ships frontmatter with `name` and `version` only, no token values, on the reasoning that inventing tokens would pre-decide what `00`'s license grant hands the tool. The linter's `missing-primary` warning is pre-accepted with that reason. Alternative was projecting the OKLCH seed as `primary`; I kept the seed in Colors prose instead so the frontmatter never outruns the lock.
- 13p handoff seam: in handoff mode the OG card and favicon stay the tool's to produce (criteria G6.6/G6.7), including in the locked regime where this skill could technically pre-produce them from the locked tokens. A killed prior iteration's uncommitted draft went the other way and was reverted under Rule 9 (its reasoning fails propose-then-lock; detail in the decisions section). If you want pre-produced share assets for locked-regime handoffs specifically, that is a small follow-up item, not a v3.0 blocker.
- 13j dogfood surfaced two docs/ findings the truth pass (19b) should settle, both in `loop/evidence/13j-dogfood/rewrite-diagnosis.md`: (1) the founder quote at `docs/index.html` line 89 is unattributed and unverifiable on a page selling a zero-fabrication guarantee; confirm it is real and attributable, or cut it. (2) The gate report row `Measurement PASS, GitHub · skills.sh` (line 129) names destinations while the page carries zero analytics wiring (grep evidence in `brief-data-pull.md`); either the claim gets grounded or the cell gets honest wording.

- F1b sequencing: the item says the fit pass runs "with the chosen direction's mockup in hand". At F1b's commit the pass fits against what Phase 4 produces today (the chosen token plan, confirmed shapes, recomposition notes); the rendered comp arrives with F2, the next item, whose own text grafts the comp in as the pass's input. Lesson 7 forbids naming a mechanism a later item owns, so the section will gain the comp reference when F2 ships. If F2 were ever dropped, revisit whether tokens-plus-shapes is enough form for the fit pass to earn its keep.
- F2 capture trap, DOGFOOD-FULL implication: the 390 render for the full dogfood (and any future Gate 5 run on this machine) must not use bare headless-Chrome `--window-size=390`; the window floors at 500 and the capture lies. Use Playwright viewport emulation or the iframe wrapper technique in `loop/evidence/audit-f2/walk.md`. Interceptor drives the real desktop Chrome window, which has the same floor, so the plan's 390/1440 Interceptor captures need the wrapper too; worth a look before the dogfood item runs.

## Blocked / deferred
(from loop/ESCALATION.md and BLOCKED fix_plan items)

## Suggested morning test script
(filled by REPORT item: how to objectively exercise v3.0, starting with rebuilding this skill's own homepage)
